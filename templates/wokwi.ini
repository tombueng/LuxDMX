# Wokwi simulation (ESP32-S3). Same as esp32s3dev but with the OLED on by default
# (dispType 1 = SSD1306 128x64) so the simulated display shows in the sim.
extends=_base
ledpin=48
ledtype=2
disptype=1
dispsda=8
dispscl=9
