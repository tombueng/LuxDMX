# Classic ESP32 (WROOM-32 / WROVER). All its old DEF_* defaults equal the global
# _base defaults (LED GPIO2 plain, display I2C 21/22, DMX 17/16, W5500 VSPI pins),
# so this template only needs to extend the base.
extends=_base
