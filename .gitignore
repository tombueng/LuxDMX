.pio/
src/generated/
docs/node_modules/
docs/video-raw/
docs/demo.mp4
docs/test-results/
docs/playwright-report/

# local editor/agent config (never publish)
.claude/
.vscode/
.history/
# stray SKiDL byproducts if lumigate.py is run from repo root
/lumigate.erc
/lumigate.log
/lumigate*_sklib.py
/lumigate*.kicad_prl

# pioarduino custom_sdkconfig from-source core build artifacts (generated from the
# custom_sdkconfig option in platformio.ini — that file is the source of truth)
/CMakeLists.txt
/sdkconfig.defaults
/sdkconfig.*
/dependencies.lock
/managed_components/
/.dummy/
/src/idf_component.yml
/src/idf_component.yml.orig

# Local-only private working assets — kept OUT of the public repo.
# (UI simulator / video pipeline + promo plan; backed up privately in tombueng/LumiGate-ops.)
/sim/
PROMO_PLAN.local.md
